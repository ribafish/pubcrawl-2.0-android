<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/background_grey_50">

    <android.support.v7.widget.Toolbar
        android:id="@+id/popup.person.toolbar"
        android:minHeight="?attr/actionBarSize"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:titleTextColor="@android:color/white"
        android:background="@color/popup_background"
        android:layout_alignParentTop="true"
        android:title="@string/participants"
        />

    <ListView
        android:id="@+id/popup.person.listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/popup.person.toolbar"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:divider="#667F7F7F"
        android:dividerHeight="2px"
        android:layout_margin="4dp"
        />


</RelativeLayout>